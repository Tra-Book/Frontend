import React, { ReactNode } from 'react'

interface HomeProps {}

const Home = ({}: HomeProps): ReactNode => {
  return <main className='flex min-h-screen flex-col items-center justify-between p-24'>Home 화면</main>
}

export default Home
