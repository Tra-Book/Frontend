import React, { ReactNode } from 'react'

interface ComponentNameProps {}

const ComponentName = ({}: ComponentNameProps): ReactNode => {
  return <div>ComponentName</div>
}

export default ComponentName
