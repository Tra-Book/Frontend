import React, { ReactNode } from 'react'

interface PlanStartRegionProps {}

const PlanStartRegion = ({}: PlanStartRegionProps): ReactNode => {
  return <div>PlanStartRegion</div>
}

export default PlanStartRegion
